import './Form.scss';

function Form() {
  return (
    <form className="form">
      <input type="text" className="form_input" />
      <button type="submit">&gt</button>
    </form>
  );
}

export default Form;
